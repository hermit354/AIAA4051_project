# Result Summaries

This directory is reserved for lightweight result artifacts that are safe to keep in GitHub.

The full experiment outputs under `outputs/` include model checkpoints, logs, and per-example prediction dumps. Those files are intentionally ignored. Keep only compact CSV/JSON summaries here when they are needed for documentation or result analysis.
